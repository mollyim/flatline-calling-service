[output]
deny = ["unmaintained", "unsound", "yanked"]
quiet = false

[advisories]
ignore = [
    # Ignore advisory that bincode is unmaintained
    "RUSTSEC-2025-0141",
    # Ignore advisory that proc-macro-error2 is unmaintained
    "RUSTSEC-2026-0173",
]
